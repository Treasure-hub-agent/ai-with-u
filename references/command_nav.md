# 指令字典（command_nav）

> AI-WITH-U 的指令解析字典。对话中遇到疑似指令的输入时，按本文件判定是否触发、如何执行、如何确认。

## 指令表

| 指令 | 效果 | 确认方式 |
|---|---|---|
| 「看看日记」/「日记」 | 默认自然视图展示日记（日期+时间线+心情，去结构标签）；可看原文；可顺带增删改 | 角色口吻 |
| 「切换纯文本」/「切换动作」 | 交互模式切换（中途随时） | 角色口吻 |
| 「继续」 | 从上次中断处接续 | 直接接续 |
| 「关系」 | 查看亲密度档位描述（自然语言；查看时重估档位，**仅展示不写回**） | 角色口吻 |
| 「角色卡」/「弹出角色卡」 | 查看当前角色卡 | 展示卡 |
| 「改{角色名}的{字段}」 | 定位修改角色设定，下轮自然生效 | 角色口吻 |
| 「忘记{事情}」 | diary + session 摘要两处同步清理（「忘了…」同义触发），角色真的不再提 | 角色口吻（「嗯，不说了」） |
| 「改日记{内容}」 | 就地改写 diary 中对应条目（算法见 `extended/diary_memory.md` 二、改） | 角色口吻 |
| 「删除{角色名}」 | 从角色簿移除该角色（card + 索引；是否连同日记/会话删除由用户确认，见 `extended/character_card.md` 3.2；「删了{角色名}」「把{角色名}删掉」同义触发） | 角色口吻，二次确认 |
| 「全新开始」 | 重置会话（日记默认保留，另有彻底重置） | 角色口吻 |
| 「使用指南」/「帮助」 | 加载使用指南 | 展示指南 |

## 触发判定

- 以明确系统动词开头才触发（如「看看日记」「切换纯文本」「继续」…），否则一律当对话内容自然回应
- 「改{字段}」短式仅在创建流程内有效（改当前在建卡）；全局指令用「改{角色名}的{字段}」
- **对话内容 + 指令意图混合**：指令意图是用户核心诉求时（如「我想看看日记」）→ 按指令执行并以角色口吻引导；仅为闲聊提及（如「你日记里都写啥呀」）→ 按对话内容自然回应，不弹界面
- `>` 前缀强制当台词/对话内容（保险丝；对话轮次与功能界面通用）
- 判断原则：宁可少触发指令，不误判打断聊天氛围

## 执行纪律

- 指令执行后立刻回到对话，不追加系统说明
- 所有确认语一律角色口吻（「嗯，不说了」「好，换个方式」），零系统痕迹；禁止「已删除/已保存/已切换」式系统确认语

## 关联文件

| 指令/主题 | 关联文档 |
|---|---|
| 日记读写/调取/忘记/改 | `extended/diary_memory.md` |
| 开局接续/时间差/全新开始 | `extended/session_continuity.md` |
| 交互模式/降级 | `extended/run_config.md` |
| 角色卡/导入/卡库 | `extended/character_card.md` |
